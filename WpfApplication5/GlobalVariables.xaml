<UserControl x:Class="VSG.GlobalVariables"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VSG"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" Name="GlobalName" Text="{Binding Path=GlobalNameB}"></TextBox>
        <TextBox Grid.Column="1" Name="GlobalValue" Text="{Binding Path=GlobalValueB}"></TextBox>
        <Button Name="DeleteGlobal" Grid.Column="2" Click="DeleteGlobal_Click">Delete</Button>
    </Grid>
</UserControl>
